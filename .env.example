# Vercel Postgres — copy from your Vercel project dashboard
POSTGRES_URL="postgres://..."
POSTGRES_PRISMA_URL="postgres://..."
POSTGRES_URL_NON_POOLING="postgres://..."
POSTGRES_USER="..."
POSTGRES_HOST="..."
POSTGRES_PASSWORD="..."
POSTGRES_DATABASE="..."

# Admin panel password (plain text — hashed on first compare)
ADMIN_PASSWORD="changeme"

# Secret for signing admin session cookie (use a long random string)
COOKIE_SECRET="change-this-to-a-long-random-secret"
